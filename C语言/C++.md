### 进入c++
```c
#include <iostream>  
using namespace std;  
  
int main() {  
    int carrots;  
    cin >> carrots;  
    cout << "Hello, World!" << endl;  
    return 0;  
}
```

包含以下元素：
* iostream：包含输入输出的多个定义
* using命名空间：不用使用std::cout 等
* cout输出和cin输入
* 换行符`endl`


